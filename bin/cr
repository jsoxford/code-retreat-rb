#!/usr/bin/env ruby
if ARGV.length > 0
    file = ARGV[0]
else
    STDOUT.puts 'You need to tell me what to watch!'
    STDOUT.puts 'Usage:'
    STDOUT.puts 'cr somefile.rb'
    Kernel.exit(true)
end

require_relative('../lib/coderetreat.rb')
cr = CodeRetreatRunner.new
cr.run(file)
